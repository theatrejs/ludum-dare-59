/**
 * @typedef {(IDLE | START)} TypeAction An action.
 */

/**
 * The 'IDLE' action.
 * @type {'IDLE'}
 * @constant
 */
const IDLE = 'IDLE';

/**
 * The 'START' action.
 * @type {'START'}
 * @constant
 */
const START = 'START';

export {

    IDLE,
    START
};
